1.  SELECT `students`.`id` AS 'id_studente', `degrees`.`name` AS 'nome_corso' 
    FROM `students`
    INNER JOIN `degrees` ON `degrees`.`id` = `students`.`degree_id`
    WHERE `degrees`.`name` = 'corso di laurea in economia';